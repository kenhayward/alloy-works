node_modules/
dist/
out/
.turbo/
coverage/
pnpm-lock.yaml
packages/api-contract/openapi.json

# Per-package Vitest JSON reports, read by `pnpm trace verify`. A build artifact of a test run,
# never committed.
.trace-results/

# Generated from openapi.json by `pnpm --filter @alloy-works/api-client generate`, and compared
# with the generator's own output in a test: formatting it would make that comparison fail.
packages/api-client/src/generated/

# Received documents, kept exactly as they arrived: one is a Word file with no parser, and the other
# uses attribute annotations Prettier rewrites into table cells.
docs/reviews/market/

# Scratch space for in-flight implementation runs: briefs, reports and review packages, generated
# from a plan and deleted when it lands. Git-ignored, but Prettier reads this file rather than
# .gitignore, and an unformatted brief must not fail `pnpm format`.
.superpowers/

# Generated from the requirement and design documents by
# `pnpm --filter @alloy-works/trace generate`, and compared with a fresh compile in a test:
# formatting it would make that comparison fail.
packages/trace/trace.json

# The evidence packs `pnpm trace pack <version>` writes. Generated, and committed - but never
# compared against a fresh run the way trace.json is: a pack records a run, not a document, and
# regenerating it later (a different commit, a rerun of the same tests) legitimately produces a
# different file. Reformatting it in place would misrepresent it as the document it was written
# from, rather than the run it actually was.
docs/trace/
